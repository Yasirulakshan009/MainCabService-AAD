/*
const RENTAL_API = "http://localhost:8080/v1/rentals";
const CUSTOMER_API = "http://localhost:8080/v1/customers";
const VEHICLE_API = "http://localhost:8080/v1/vehicles";

let rentalList = [];
let customerList = [];
let vehicleList = [];

let editingRentalId = null;

function getAuthHeaders() {
    const token = localStorage.getItem("jwtToken");

    return {
        "Authorization": "Bearer " + token
    };
}*/
